diff --git a/js/app.js b/js/app.js
index 2b15bb4..2eefaf2 100644
--- a/js/app.js
+++ b/js/app.js
@@ -63,8 +63,8 @@
         '</div>' +
         '<div class="term-side">' +
           '<span class="badge">' + esc(it.topic) + '</span>' +
-          '<span class="badge bad">오답 ' + w.c + '</span>' +
-          '<span class="badge ok">연속정답 ' + (w.s || 0) + '/' + Store.MASTER_STREAK + '</span>' +
+          '<span class="badge bad">오답 ' + (Number(w.c) || 0) + '</span>' +
+          '<span class="badge ok">연속정답 ' + (Number(w.s) || 0) + '/' + Store.MASTER_STREAK + '</span>' +
           '<button class="linkbtn js-del" type="button">삭제</button>' +
         '</div></li>';
     }).join('');
@@ -85,16 +85,18 @@
       ['연속 학습', s.streak + '일']
     ];
     el.statGrid.innerHTML = cards.map(function (c) {
-      return '<div class="stat"><div class="stat-num">' + c[1] +
-             '</div><div class="stat-label">' + c[0] + '</div></div>';
+      return '<div class="stat"><div class="stat-num">' + esc(c[1]) +
+             '</div><div class="stat-label">' + esc(c[0]) + '</div></div>';
     }).join('');
 
     var ts = s.topicStat;
-    var rows = global.Dict.TOPIC_ORDER.filter(function (t) { return ts[t] && ts[t].n; });
+    var rows = global.Dict.TOPIC_ORDER.filter(function (t) { return ts[t] && Number(ts[t].n) > 0; });
     el.topicStats.innerHTML = rows.length ? rows.map(function (t) {
-      var v = ts[t], pct = Math.round(v.k / v.n * 100);
+      var v = ts[t];
+      var n = Number(v.n) || 0, k = Number(v.k) || 0;
+      var pct = n ? Math.max(0, Math.min(100, Math.round(k / n * 100))) : 0;
       return '<div class="bar-row">' +
-        '<div class="bar-top"><span>' + esc(t) + '</span><b>' + pct + '% (' + v.k + '/' + v.n + ')</b></div>' +
+        '<div class="bar-top"><span>' + esc(t) + '</span><b>' + pct + '% (' + k + '/' + n + ')</b></div>' +
         '<div class="bar"><div class="bar-fill" style="width:' + pct + '%"></div></div></div>';
     }).join('') : '<p class="hint">아직 푼 문제가 없다.</p>';
   }
diff --git a/js/dictionary.js b/js/dictionary.js
index 6a5adef..1cf9cb5 100644
--- a/js/dictionary.js
+++ b/js/dictionary.js
@@ -119,7 +119,7 @@
         '</div>' +
         '<div class="term-side">' +
           '<span class="badge">' + esc(it.topic) + '</span>' +
-          (w ? '<span class="badge bad">오답 ' + w.c + '</span>' : '') +
+          (w ? '<span class="badge bad">오답 ' + (Number(w.c) || 0) + '</span>' : '') +
           '<span class="star' + (Store.isFav(it.id) ? ' is-on' : '') + '">' +
             (Store.isFav(it.id) ? '★' : '☆') + '</span>' +
         '</div>' +
diff --git a/js/storage.js b/js/storage.js
index c1a292e..d9d9b23 100644
--- a/js/storage.js
+++ b/js/storage.js
@@ -22,19 +22,80 @@
 
   var state = load();
 
-  function load() {
+  /* ----- 저장값 정규화 -----------------------------------------------------
+     localStorage 는 앱 밖에서도 고쳐 쓸 수 있는 영역이다. 읽어온 값을 그대로
+     state 에 넣으면 숫자를 기대한 자리에 문자열이 들어오고, 그 값이 화면
+     렌더에서 문자열 연결로 innerHTML 에 실려 태그가 주입될 수 있다.
+     그래서 읽는 시점에 형태와 타입을 강제한다.
+     ---------------------------------------------------------------------- */
+  function num(v) {
+    var n = Number(v);
+    return isFinite(n) ? n : 0;
+  }
+  function isPlainObject(v) {
+    return !!v && typeof v === 'object' && !Array.isArray(v);
+  }
+  function idList(v) {
+    if (!Array.isArray(v)) return [];
+    var out = [], seen = {};
+    v.forEach(function (x) {
+      var n = Number(x);
+      if (!isFinite(n) || seen[n]) return;
+      seen[n] = true;
+      out.push(n);
+    });
+    return out;
+  }
+
+  function sanitize(saved) {
     var base = JSON.parse(JSON.stringify(DEFAULTS));
+    if (!isPlainObject(saved)) return base;
+
+    base.favorites = idList(saved.favorites);
+    base.mastered = idList(saved.mastered);
+
+    if (isPlainObject(saved.wrong)) {
+      Object.keys(saved.wrong).forEach(function (k) {
+        var id = Number(k);
+        if (!isFinite(id)) return;
+        var w = saved.wrong[k];
+        if (!isPlainObject(w)) return;
+        base.wrong[id] = { c: num(w.c), s: num(w.s), t: num(w.t) };
+      });
+    }
+
+    if (Array.isArray(saved.history)) {
+      base.history = saved.history
+        .filter(isPlainObject)
+        .slice(0, 60)
+        .map(function (h) {
+          return { d: String(h.d == null ? '' : h.d).slice(0, 10), n: num(h.n), k: num(h.k) };
+        });
+    }
+
+    if (isPlainObject(saved.topicStat)) {
+      Object.keys(saved.topicStat).forEach(function (k) {
+        var t = saved.topicStat[k];
+        if (!isPlainObject(t)) return;
+        base.topicStat[String(k).slice(0, 20)] = { n: num(t.n), k: num(t.k) };
+      });
+    }
+
+    base.totalAsked = num(saved.totalAsked);
+    base.totalCorrect = num(saved.totalCorrect);
+    base.theme = (saved.theme === 'light' || saved.theme === 'dark') ? saved.theme : null;
+    return base;
+  }
+
+  function load() {
     try {
       var raw = global.localStorage.getItem(KEY);
-      if (!raw) return base;
-      var saved = JSON.parse(raw);
-      Object.keys(base).forEach(function (k) {
-        if (saved[k] !== undefined && saved[k] !== null) base[k] = saved[k];
-      });
+      if (!raw) return sanitize(null);
+      return sanitize(JSON.parse(raw));
     } catch (e) {
       console.warn('[storage] 불러오기 실패, 초기값 사용:', e);
+      return sanitize(null);
     }
-    return base;
   }
 
   function save() {
@@ -75,7 +136,7 @@
 
     markWrong: function (id) {
       var w = state.wrong[id] || { c: 0, s: 0, t: 0 };
-      w.c += 1;
+      w.c = num(w.c) + 1;
       w.s = 0;
       w.t = Date.now();
       state.wrong[id] = w;
@@ -88,7 +149,7 @@
     markCorrect: function (id) {
       var w = state.wrong[id];
       if (!w) return false;
-      w.s = (w.s || 0) + 1;
+      w.s = num(w.s) + 1;
       if (w.s >= MASTER_STREAK) {
         delete state.wrong[id];
         if (state.mastered.indexOf(id) === -1) state.mastered.push(id);
